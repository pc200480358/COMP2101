#!/bin/bash
# going to bash directory
#hostname -f
#distinguish devices within local network
#hostnamectl
#peovide proper API and change related settings
#hostname -l
#distinguish devices within local network
#df -h | grep -w /
