#!/bin/bash
#going to bash directory
hostname -f
#distinguish devices within local network
hostname ctl
#provide proper API and change related settings
hostname -l
#distinguish devices within local network
df -h | grep -w /
